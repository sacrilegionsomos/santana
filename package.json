{
  "name": "bhd-firebase-server",
  "version": "4.1.0",
  "description": "Servidor BHD con Firebase y Telegram - Con botones interactivos y diagnóstico mejorado",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup-webhook": "node -e \"require('axios').get(process.env.RENDER_URL + '/setup-webhook').then(console.log).catch(console.error)\"",
    "test-telegram": "node -e \"require('axios').get(process.env.RENDER_URL + '/test-telegram').then(console.log).catch(console.error)\"",
    "health": "node -e \"require('axios').get(process.env.RENDER_URL + '/health').then(r => console.log(JSON.stringify(r.data, null, 2))).catch(console.error)\""
  },
  "keywords": [
    "bhd",
    "firebase",
    "telegram",
    "bot",
    "server",
    "webhook",
    "monitoring"
  ],
  "author": "sacrilegionsomos",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^11.11.1",
    "axios": "^1.6.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tu-usuario/bhd-firebase-server.git"
  }
}